name: 'ev_grid'
version: '1.0.0'
profile: 'ev_grid'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ev_grid:
    staging:
      +materialized: view
    marts:
      +materialized: table
